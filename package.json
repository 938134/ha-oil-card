{
  "name": "ha-oil-card",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp src/oil-card.js dist/oil-card.js",
    "release": "standard-version"
  },
  "devDependencies": {
    "standard-version": "^9.5.0"
  }
}
